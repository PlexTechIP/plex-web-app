import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join | PlexTech - Berkeley",
};

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
